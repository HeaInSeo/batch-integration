apiVersion: v1
kind: Service
metadata:
  name: dev-space-observability
  namespace: dev-space
spec:
  selector:
    app: dev-space-observability
  ports:
    - name: http
      port: 80
      targetPort: http
